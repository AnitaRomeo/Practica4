pipeline {
    agent any 
    stages{
        stage('listar directorio actual'){
            steps{
                sh 'ls -la'
            }
        }
        stage('build de docker'){
            steps{
                sh 'cd deploy'
                sh 'ls -la'
                sh 'docker build -t web:latest . '
            }
        }
    }
}